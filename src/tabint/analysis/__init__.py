"""Analysis feature — data-analysis tools, service (workspace + algorithms),
and db (DuckDB mechanics). Also owns the Session facade, on-disk persistence,
and the session registry the MCP/CLI surfaces build on.
"""
