"""Outreach feature — templates → campaigns → prospect emails.
Owns its MCP tools (CRUD over the user's account via the integration client)
and the outreach-agent prompt. Does NOT send email — sending is the user's own
email tool/MCP.
"""
